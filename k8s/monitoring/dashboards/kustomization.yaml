apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

configMapGenerator:
  - name: news-lab-pipeline-operations-dashboard
    files:
      - news-lab-pipeline-operations.json

generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"
