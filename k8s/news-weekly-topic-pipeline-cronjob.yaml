apiVersion: batch/v1
kind: CronJob
metadata:
  name: news-weekly-topic-pipeline
spec:
  schedule: "30 0 * * 1"
  timeZone: "Asia/Seoul"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 1800
      backoffLimit: 1
      template:
        metadata:
          labels:
            app: news-weekly-topic-pipeline
        spec:
          automountServiceAccountToken: false
          restartPolicy: Never
          nodeSelector:
            workload: app
          containers:
            - name: news-weekly-topic-pipeline
              image: seocj/news-api:7636ee0db92d8fcbf2111688febea2e90edf54a1
              imagePullPolicy: Always
              command:
                - python
                - -u
                - scripts/run_weekly_topic_pipeline.py
                - --max-articles
                - "1000"
                - --similarity-threshold
                - "0.70"
                - --max-topics
                - "5"
                - --max-related-articles-per-topic
                - "20"
                - --max-summary-articles-per-topic
                - "5"
                - --max-raw-chars-per-article
                - "3000"
                - --use-summary-provider
                - --summary-model
                - gpt-5-nano
                - --execute
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
                seccompProfile:
                  type: RuntimeDefault
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: news-api-secret
                      key: DATABASE_URL
                - name: OPENAI_SUMMARY_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: news-api-secret
                      key: OPENAI_SUMMARY_API_KEY
              resources:
                requests:
                  cpu: "100m"
                  memory: "128Mi"
                limits:
                  cpu: "500m"
                  memory: "512Mi"
          volumes:
            - name: tmp
              emptyDir: {}
