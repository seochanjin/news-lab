apiVersion: batch/v1
kind: CronJob
metadata:
  name: news-three-day-topic-pipeline
spec:
  schedule: "0 5 * * *"
  timeZone: "Asia/Seoul"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 1800
      backoffLimit: 1
      template:
        metadata:
          labels:
            app: news-three-day-topic-pipeline
        spec:
          restartPolicy: Never
          nodeSelector:
            workload: app
          containers:
            - name: news-three-day-topic-pipeline
              image: seocj/news-api:5cbb040f3efe858c7a898ddae611f00ad1d2aeb5
              imagePullPolicy: Always
              command:
                - python
                - -u
                - scripts/run_three_day_topic_pipeline.py
                - --window-hours
                - "72"
                - --max-articles
                - "500"
                - --similarity-threshold
                - "0.70"
                - --max-topics
                - "5"
                - --max-related-articles-per-topic
                - "20"
                - --max-summary-articles-per-topic
                - "3"
                - --max-raw-chars-per-article
                - "3000"
                - --use-summary-provider
                - --summary-model
                - gpt-5-nano
                - --execute
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
                seccompProfile:
                  type: RuntimeDefault
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: news-api-secret
                      key: DATABASE_URL
                - name: OPENAI_SUMMARY_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: news-api-secret
                      key: OPENAI_SUMMARY_API_KEY
              resources:
                requests:
                  cpu: "100m"
                  memory: "128Mi"
                limits:
                  cpu: "500m"
                  memory: "512Mi"
          volumes:
            - name: tmp
              emptyDir: {}
